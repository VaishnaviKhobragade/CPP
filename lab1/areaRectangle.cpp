// Calculate area of rectangle
#include<iostream>
using namespace std;
int main()
{
	float width,length,area;
	cout<<"Accept the width and length from user "<<endl;
	cin>>width>>length;
	area=width*length;
	cout<<" Area of rectangle is  "<<area;
}
